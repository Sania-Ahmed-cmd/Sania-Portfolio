# TODO - Origin Story Interactive Lore Cards

- [x] Update `index.html` to make each ORIGIN STORY `.about-card` clickable, add `data-origin-arc` identifiers, add “CLICK TO OPEN FILE” badge, and insert hidden “CLASSIFIED FILE” content blocks for each arc.

- [x] Update `style.css` with new comic-dossier flip/unfold animations and styling for the hidden content; ensure mobile responsiveness.


- [x] Update `script.js` to toggle open/closed state on card click (and via keyboard), without breaking existing click-bubbles and other functionality.

- [x] Verify by running/opening the page: click-to-open/close works, content reveals correctly per arc, animations are smooth, mobile layout remains intact.


