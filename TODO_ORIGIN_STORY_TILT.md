# TODO_ORIGIN_STORY_TILT

- [ ] Remove 3D tilting from origin overlay in `style.css` (`.origin-file` default + `.about-card.is-open .origin-file`).
- [ ] Remove hover-based tilting/rotation from origin cards in `script.js` (the `mouseenter`/`mouseleave` that sets `card.style.transform`).
- [ ] Verify click-to-open still works and overlay unfolds without 3D transform.

